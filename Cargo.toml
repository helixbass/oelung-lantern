[package]
name = "oelung-lantern"
version = "0.1.0"
edition = "2024"

[dependencies]
crossterm = "0.29.0"
oelung = { path = "../oelung" }
tokio = { version = "1", features = [ "full" ] }

[dev-dependencies]
anyhow = "1.0.100"
